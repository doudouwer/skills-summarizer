# Copy to .env and set values. Agent-specific vars take precedence; falls back to OPENAI_* if unset.

# Required: set at least one of API key below
SKILL_SUMMARIZER_OPENAI_API_KEY=
# OPENAI_API_KEY=

# Optional: base URL (e.g. for proxy or OpenAI-compatible API)
SKILL_SUMMARIZER_OPENAI_BASE_URL=https://api.openai.com/v1
# OPENAI_BASE_URL=

# Optional: model and generation
SKILL_SUMMARIZER_OPENAI_MODEL=gpt-5.2
SKILL_SUMMARIZER_TEMPERATURE=0.3
SKILL_SUMMARIZER_MAX_TOKENS=4096

# Optional: default paths (can override at runtime)
# SKILL_SUMMARIZER_PROJECT_ROOT=
